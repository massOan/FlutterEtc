import 'package:flutter/material.dart';
class FooterBusinessInformationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('사업자 정보'),
      ),
      body: Center(
        child: Text('사업자 정보 내용'),
      ),
    );
  }
}